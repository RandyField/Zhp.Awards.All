@{
    ViewBag.Title = "Index";
}
@using Model
@model wxUserInfoModel
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>获取用户信息</title>
    <script src="https://cdn.bootcss.com/jquery/1.3.1/jquery.min.js"></script>
</head>

<body>

    <table>
        <tr>
            <td>昵称</td>
            <td>性别</td>
            <td>头像</td>
            <td>国家</td>
            <td>省份</td>
            <td>城市</td>
        </tr>
        <tr>
            <td>@Model.Nickname</td>
            <td>@Model.Sex</td>
            <td>@Model.Headimgurl</td>
            <td>@Model.Country</td>
            <td>@Model.Province</td>
            <td>@Model.City</td>

        </tr>
    </table>
</body>
@*<script type="text/javascript">

    $(function () {
        var getCode = GetQueryString("code");
        var url = 'WxGetToken/GetToken'
        $("#codes").text(getCode);
        alert(getCode);
        var data = [];
        data["code"] = getCode;
        $("#btn1").click(function () {
            $.get(url, data,function (data) {
                alert("Data Loaded: " + data);
            }).done(function (data) {
                alert(data)
            });   
        });
    });
    //过滤url参数
    function GetQueryString(c) {
        var reg = new RegExp("(^|&)" + c + "=([^&]*)(&|$)");
        var r = window.location.search.substr(1).match(reg);
        if (r != null)
            return unescape(r[2]);
        return null;
    }
</script>*@



</html>

