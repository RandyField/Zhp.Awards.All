<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="initial-scale=1, maximum-scale=3, minimum-scale=1, user-scalable=no">
    <title>运气不好，下次再来吧！</title>
    <link href="~/Content/css/main.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <style type="text/css">
        .tips {
            position: absolute;
            width: 4.1rem;
            height: 1.5rem;
            top: 1.1rem;
            left: calc(50% - 2.08rem);
            font-size: 0.24rem;
        }

        #nickName {
            position: absolute;
            width: 6.5rem;
            height: 1.5rem;
            top: 0.6rem;
            left: calc(50% - 3.25rem);
            font-size: 0.50rem;
            text-align: center;
        }

        #wx-name {
            color: #f42573;
            font-family: '微软雅黑';
            margin-left: auto;
            margin-right: auto;
        }

        .ipt {
            position: absolute;
            width: 5.5rem;
            height: 1.5rem;
            top: 0.1rem;
            left: calc( 50% - 2.75rem);
            left: -webkit( 50% - 1.75rem);
        }

            .ipt div.sure {
                margin-top: 0.2rem;
                background: #ffd619;
                font-weight: bold;
                font-size: 0.37rem;
                text-align: center;
                color: #fefefe;
            }

        #takeAwayBtn {
            cursor: pointer;
        }
    </style>
</head>
<body class="bg">
    <img src="/wxredpackets/Content/image/GDBank/hadreceived.jpg" id="topImg" style="width: 100%; height: 100%;">
    <script src="~/Scripts/jquery-1.11.1.min.js" type="text/javascript" charset="utf-8"></script>
    <script src="~/Scripts/js/rem.js" type="text/javascript"></script>
    <script type="text/javascript">
        window['adaptive'].desinWidth = 720; // 设计图宽度
        window['adaptive'].maxWidth = 540; // 页面最大宽度 默认540
        window['adaptive'].init(); // 调用初始化方法
        $('body').show();
        $('body').height($('body').get(0).clientHeight + 'px ');
        setTimeout(function () { WeixinJSBridge.call('closeWindow'); }, 5000);
    </script>
</body>
</html>
